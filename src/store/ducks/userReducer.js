// Assign nickname, call currentRank & Leaderboard at same time ish

const initialState = {
    leaderboard: [],
    currentRank: {}
}

export default function userReducer(state = initialState, action){
    return state
}